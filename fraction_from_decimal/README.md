# fraction_from_decimal

Returns the simplest fraction that resolves to a given decimal rounded to a 5 digit precision after the decimal point. The return value is string representation of the fraction -- i.e., `fraction_from_decimal(0.33333) = '1/3'`).
